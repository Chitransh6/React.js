___Video 3___

1. use import export to use component in onother file
  -- default import-export
      -only can export one component
      syntax - export default componentName
             - import componentName from "path"

  -- named import-export
    --can export miltiple component
      syntax -export componentFunction
             -import {componentName} from "path"

2. JXS(javascript XML) is html like syntax language.it used in component.it used   to manipulate DOM using js.it is note html inside javascript.

3. i inserted react in my project using vite bundler.

4. React-DOM is utility of react.we insert this when we work on web.

5. always keep first letter of component name in capital latter.

6. we can use component by writing <componentName/>

7. we can use this multiple time

__video 4__

1. react.creatElement('type',{atributs},'children');
  -- by using this we create the eleemnt in react

2. at the end React render elements as javascript object

3. in render function jsx code are converted into javascript object

4. createElement are given by the bebal
   -babel transpiled our react code into js engine or browser understable language

5. parcel is the is the bundler is is very useful for our project;
    - it handles error for project
    - use for dignostic
    - hot page reloading
    - it builds local host sevver for project
    - it is very fast for its work
    - it use cache to build server in faster then previous time
    - use for image optimization
    - compress the size of file
    - transpiled the code for browser

    __video 5__

1. Hooks these are the inbuild functions that we use in react
   
   -- useState => it used to update variables on UI, in general when we udate the verible it does not shows in UI; it makes vary easy veriable updation

     Syntax -  let[state,setState] = useState(initial value)
              
              - useState returns a array that has two things
                 first- current value of veriable
                 second- function, that moniter the update

2. in this class i made a simple counter project that increase the value of counter by one if i click on add value button and decrease the value of counter by one if i clicks on subValue button and  we show the updation of counter on UI by using useState;




   



             

